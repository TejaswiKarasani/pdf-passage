import { authResult } from '@passageidentity/passage-js';
export { authResult } from '@passageidentity/passage-js';

type BeforeAuthCallback = (email: string) => boolean;

type OnSuccessCallback = (authResult: authResult) => void;

interface PassageElement extends HTMLElement {
    appId?: string;
    onSuccess?: OnSuccessCallback;
    beforeAuth?: BeforeAuthCallback;
    lang: string;
    defaultCountryCode?: string;
}

interface PassageProfileElement extends HTMLElement {
    appId?: string;
    lang: string;
    defaultCountryCode?: string;
}

export { BeforeAuthCallback, OnSuccessCallback, PassageElement, PassageProfileElement };
